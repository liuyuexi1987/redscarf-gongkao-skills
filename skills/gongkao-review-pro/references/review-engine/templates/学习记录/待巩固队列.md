# 待巩固队列

> 主动提问形成的轻量索引，不等于错因或弱点。运行时复制为 `待巩固队列.md`，同一主题合并更新。

| 主题键 | 模块/题型 | 用户表现 | 求助次数 | 最近日期 | 状态 | 下次复习问法 |
| --- | --- | --- | ---: | --- | --- | --- |

状态仅使用：`待巩固` / `已理解` / `转弱点`。只有 `待巩固` 参与普通“考我/复习”取题。

“下次复习问法”以 `【开放题，无选项】` 开头，保存一条可直接向用户提出的完整纯概念问题；只问方法选择、触发词、陷阱或关键步骤，不含新编数字、材料和选项。
